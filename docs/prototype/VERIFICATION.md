# 验证记录

日期：2026-09-08。

`npm run build` 成功；`npm test` 共 14 项通过（11 项业务、3 项 DOM 交互）。
企业抬头税号必填：业务测试覆盖缺失、空白拒绝及有效税号通过；
DOM 测试覆盖切换企业/个人时必填属性、标签及有效性同步更新。
测试运行于 Node.js / jsdom，不启动任何服务，不访问业务数据库。

| 需求 | 实现与证据 |
| --- | --- |
| React、顶部导航、独立 CSS | React 打包成功；DOM 验证四个顶部导航入口；样式独立引入 |
| 全局模糊搜索全部结果 | 业务测试验证大小写、关联提供商抬头、回收站和无截断 |
| 跳转并高亮 1 秒 | DOM 测试跳转回收站，验证滚动定位和 1 秒后高亮移除 |
| 订单全部加载 | DOM 验证初始 30 条正常订单；table-scroll 隐藏滚动条并限制高度 |
| 搜索、筛选在前端 | 搜索纯函数和页面过滤逻辑，不存在网络请求 |
| 订单五类筛选 | 更新时间起止、提供商、支付方式、发票状态、类型控件及过滤逻辑 |
| 列表字段与编号复制 | 表格字段完整；DOM 验证复制回调内容 |
| 发票和抬头字段入口 | DOM 点击发票状态打开详情；点击类型打开包含税号的抬头详情 |
| 订单新增与修改 | DOM 完成新增及金额修改，业务层校验金额与枚举 |
| 已开票和回收订单限制 | 业务测试验证禁止操作与批量原子性；DOM 验证禁用按钮 |
| 合并开票与作废 | DOM 完成两订单开票、关联列表查询、作废解绑；业务验证整数分合计 |
| 回收、恢复、永久删除 | DOM 验证单条闭环；业务测试验证批量与位置限制 |
| 发票列表、筛选、详情 | DOM 验证发票页、四个筛选控件、关联列表和作废状态 |
| 提供商卡片操作 | DOM 验证新增、修改、批量删除；网址文字非链接且独立新页入口 |
| 抬头卡片操作 | 共用表单和删除流程；DOM 验证新增，业务覆盖抬头保存与关联删除 |
| 提供商/抬头关联删除 | 业务测试允许删除，缺失引用安全显示，UUID 避免旧引用误接管 |
| 日期与输入限制 | 业务测试覆盖长度、网址、税号、未来日期、本地时间和金额 |
| 可恢复初始数据 | 重置按钮经确认调用 seedData；localStorage 保存原型状态 |

## 未验证范围

浏览器工具因 URL 安全策略拒绝打开本地文件。未绕过该限制，未启动预览服务。
尚无真实浏览器截图验收；原生 dialog 焦点约束、剪贴板权限、滚轮效果、
响应式视觉效果和系统辅助功能仍需人工浏览器检查。

建议人工打开 `index.html`，检查列表内滚动、弹窗、顶部导航和视觉间距；
再尝试筛选未开票订单、选择两条开票、查看发票和作废。
“重置示例数据”可清除验收产生的模拟更改。
